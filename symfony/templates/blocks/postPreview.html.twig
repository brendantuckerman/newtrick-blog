{# postPreview.html.twig #}
{% macro renderPostPreview(post) %}
    <div class="post-preview w-full">
        <h2>{{ post.title }}</h2>
        <p>{{ post.user.username }}</p>
        <a href="/">Read more</a>
    </div>
{% endmacro %}